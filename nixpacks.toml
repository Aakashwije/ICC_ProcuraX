# Nixpacks config for Railway deployment
# The backend lives in procurax_backend/

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd procurax_backend && npm ci --omit=dev"]

[start]
cmd = "cd procurax_backend && node app.js"
